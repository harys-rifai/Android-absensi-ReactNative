# Fixes Needed:
1. Remove double setIsSubmitting(true) in submitAttendance
2. Remove site selection UI (site comes from user.site_id)
3. Move check-in/out/sync buttons UNDER calendar
4. Create Requests menu (cuti, lembur, late, off)
5. Add approval workflow (line manager + HRD)
6. Update database schema for approvals
